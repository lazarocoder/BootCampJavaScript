// function with multible arguments

/*let multi = function (x, y, c) {
  return (x + y) / c
}

let test = multi(10, 15, 2)
console.log(test)

// default arguments 
let test = function (name= 'test for the new name', score = 10) {
  console.log(name)
  console.log(score)  

}

test()

let test = function (name='test', score=100) {
return 'Name :' + name + 'Score : ' + score

}
let resultTest = test()
console.log(resultTest)

let bill = function (totalBill, tipPercent) {
return totalBill * tipPercent 
}

let response = bill(40, 0.25)
console.log('The tipPercent is: ' + response)*/


/*let age = function (oYear, nYear) {

  let result = nYear - oYear
  if (result < 36) {
    return `you are so young!`

  } else {
    return result
  }
}

let test = age(1987, 2055)
console.log(test)*/





